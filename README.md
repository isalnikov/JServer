text
<!-- QWEN-CODE-CLI: template --> 

# [НАЗВАНИЕ ПРОЕКТА : "JServer"]

## Обязательные секции для заполнения

### 1. Описание проекта
[Краткое описание того, что делает проект, его цели и задачи: 
 Проект высоконагруженного java сервера , который может обрабатывать по 200 000 rps 
 Код храним в git.
]

### 2. Требования к функциональности
[Перечислите основные функции, которые должны быть реализованы .В виде списка:
- [ ] асинхронный не блокирующий высокопроизводительный сервер на писанный на чистом java 25  с использованием `Virtual Threads`
- [ ] асинхронный Логирование всех операций
- [ ] Rate limiting для API
- [ ] Health check endpoints


]

### 3. Технические требования
- Базово создаем сначала Модели C4  (c4 model )
- Язык программирования: [Java 25+  Использовать все современные фичи]
- Зависимости: [maven и pom.xml для сборки и зависимостей] 
- Нельзя использовать lombok, мы пишем чистый и понятный код без магии!
- База данных: [локальной H2 ]Не тянуть в код лишние библиотеки без надобности , если можно написать свой простой и читаемый код]
- Платформа: [ОС, linux]


### 4. Архитектура и структура проекта
[Опишите, как должен быть организован код, какие модули, классы, функции должны быть, ожидаемая структуру папок и файлов:

- Создаем сначала Модели C4  (c4 model )
- стурктура проекта:
тут простая понятная структура кода 



**Соглашения по коду**:
- Использовать `record` для DTO, `class` для JPA-сущностей.
- Внедрение зависимостей через конструктор (final поля).
- Логирование: `private static final Logger logger = LoggerFactory.getLogger(ClassName.class);`
- Использовать современные возможности Java: `var`, `switch expressions`, `text blocks`, `Pattern Matching for instanceof`, `Sequenced Collections`, `Virtual Threads` (где уместно).

**Примечания для генерации кода**:
- Сначала создаем Модели C4
- Код должен быть чистым, читаемым, без лишних абстракций.
- Код должен быть очень качественно комментирован ( язык русский) полнота javadoc 100%.
- Каждый класс должен иметь одну ответственность.
- Логирование везде по коду через private static final Logger logger = LoggerFactory.getLogger(ClassName.class);.


]

### 5. API (если применимо)
[endpoints, методы, форматы запросов и ответов:
Основные эндпоинты (префикс `/api/v1`):


]

### 6. Пример использования
[Приведите примеры команд или кода, демонстрирующие использование:
curl-команды и примеры ответов


/start              — Приветствие и меню (Запустить бота)
/version		    — Показать информацию о сборке,показывает версию, время сборки и номер билда.
/help               — Справка по командам
]

### 7. Установка и запуск
[Инструкции по установке зависимостей, настройке окружения, запуску:
сборка одной командой из терминала, в командной строке указать параметры и ключи для старта, токены , все необходимые параметры для запуска

# Запуск с параметрами ( пример )
java -jar jserver.jar
]

### 8. Переменные окружения/конфигурация
[Список переменных, которые нужно задать: Все параметры могут быть заданы в application.yml или через переменные окружения]

### 9. Тестирование
[Как запускать тесты, какие тесты нужны:
Всю разработку веди исключительно через тесты и TDD, покрыть не менее 90% кода тестами, писать все виды тестов.  
Использовать mock из Mockito.  
JUnit 5 и junit jupiter для выполнения модульных тестов.
jacoco для измерения покрытия Java-кода тестами
Логирование, простое и понятное без магии аннотаций, логируем все операции и действия в консоль и в файл, логирование удобно настраиваемое, делаем ротацию логов, создание логера во всех классах через private static final Logger logger = LoggerFactory.getLogger(this.class); Мы не используем System.out.println()]

### 10. Лицензия
[Лицензия MIT]

## Примечания для генерации кода
[Сначала создаем Модели C4
Всю разрбаотку веди исключительно через тесты и TDD.
SOLID — каждый класс имеет одну ответственность
DRY — нет дублирования кода
KISS — код простой и понятный
YAGNI — не добавляем лишнюю функциональность
TDD — тесты пишутся до кода.
Используй все свои skills , если они применимы.
При написании кода, помни про SOLID, DRY, KISS, YAGNI.
Каждый раз, когда пишешь код, думай как его улучшить, сделать простым, понятным легко читаемым


Тесты
Unit тесты на Service слой (90%+)
Проверка нагрузки 

]
